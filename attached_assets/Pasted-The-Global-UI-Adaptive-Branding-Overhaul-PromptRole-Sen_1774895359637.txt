The Global UI & Adaptive Branding Overhaul PromptRole: Senior UI/UX EngineerTask: Universal Interface Visibility, Adaptive Branding, and Theme Logic"I need a comprehensive, app-wide UI overhaul to fix visibility and branding issues. This must apply to every single page, including the Sign-In screen, all Tool categories, the Projects/Subscription pages, and every individual Calculator. Execute the following:Adaptive Branding Update: Remove the text 'ArcSide TM professional welding platform' from the header. Replace it with a centered ArcSide logo only. Because the app supports light and dark modes, apply a subtle, neutral outer glow (low spread) to the logo so it remains clearly visible against both black and white backgrounds.Universal Button Visibility: All buttons (Sign-In, Analyze, Generate, Upgrade, Contact Sales) must no longer appear as flat text. Apply a Royal Blue ($#002366$) background with white text, a $2px$ border, and a $5px$ corner radius. Add a subtle drop shadow to ensure they are clearly defined as clickable elements.Calculator & Field Refinement: On all calculators (e.g., Wire Feed Speed, Heat Input, Gas Flow Rate), the input fields and buttons are currently blending into the background. Apply high-contrast borders to all input fields and ensure the 'Calculate' buttons follow the new Royal Blue button styling.AI Assistant Icon: Locate the 'Sparkle' AI assistant icon. Increase its scale by 25% and wrap it in a circular floating action button (FAB) container with a high-contrast background so it stands out prominently.Settings & Toggles: Refactor the appearance and color settings. Replace flat text labels with functional toggle switches or segmented control buttons. Ensure the 'Active' state is visually distinct from the 'Inactive' state.Theme Logic (Light/Dark Mode): * Dark Mode: Keep Royal Blue buttons with high-intensity white text and subtle depth.Light Mode: Keep Royal Blue buttons with crisp white text. Add a slightly darker $1px$ border around input fields to prevent them from washing out against white backgrounds.Localization & Units: Confirm all currency symbols are in Rands (R) and all measurements are strictly Metric across all pages.Apply these styles globally to ensure 100% consistency. Confirm once the CSS and component logic have been updated across all environments.